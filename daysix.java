import java.util.*;
class daysix
{
	/*public static void main(String args[])
	{
		String pname="teacups";
		char pcode='t';
		int pprice=25;
		Scanner sc=new Scanner(System.in);
		System.out.print("Enter NO OF Quantity : ");
		int pquantity=sc.nextInt();
		double ptotal=pprice*pquantity;
		double finalprice=0;
		double discount=0;
		

		if(ptotal>=1000 && ptotal<=2000)
		{
			discount=ptotal/100*10;
			finalprice=ptotal-discount;
			
		}
		else if(ptotal>2000 && ptotal<=3000)
		{
			discount=ptotal/100*15;
			finalprice=ptotal-discount;
		}
		else if(ptotal>3000 && ptotal<=4000)
		{
			discount=ptotal/100*20;
			finalprice=ptotal-discount;
		}
		else if(ptotal>4000)
		{
			discount=ptotal/100*30;
			if(discount>=1400)
			{
				finalprice=ptotal-1400;
			}
			else
				finalprice=ptotal-discount;
		}
		else
		{
			discount=0;
			finalprice=ptotal;
		}
		double tax=finalprice/100*18;
		finalprice=finalprice+tax;
		System.out.println("CODE: " +pcode +"  " +" NAME :" +pname +" " +" QUANTITY :"  +pquantity +" " +" DISCOUNT " +discount +" TAX: " +tax +" FINAL AMOUNT :" +finalprice);
	}*/
	public static void main(String args[])
	{
		double a=10.5;
		int b=(int)a;	
		
		System.out.println(b+' ' +a);
	}
}
		